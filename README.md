use the command
```bash
chmod +x generate_all_programs.sh
./generate_all_programs.sh
```
This will instantly create all Program1.c through Program54.c files in the current directory you run the command in